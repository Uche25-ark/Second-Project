// import fs from "fs";

// const image = fs.readFileSync("./images/shoe.png");

// const base64 = image.toString("base64");

// const finalImage = `data:image/png;base64,${base64}`;

// // show only first 10 characters
// console.log(finalImage.slice(0, 49));